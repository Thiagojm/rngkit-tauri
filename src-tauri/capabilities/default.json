{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the main window: core IPC and native dialogs only",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
